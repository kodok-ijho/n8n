const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

app.get('/', (req, res) => {
  res.send('WAHA running on Railway!');
});

app.listen(PORT, () => {
  console.log(`WAHA listening on port ${PORT}`);
});
